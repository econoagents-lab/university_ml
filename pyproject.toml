[project]
name = "machine-learning-university"
version = "0.1.0"
description = "Universidad ejecutable de Machine Learning aplicada a inteligencia comercial inmobiliaria."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
